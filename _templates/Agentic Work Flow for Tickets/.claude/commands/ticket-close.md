Close a ticket. Args: KEY. HUMAN-GATED: ask the human to confirm the deploy was executed and
verified (results pasted into 04-test-evidence.md / deploy-steps.md).
Verify all artifacts exist and gates 1-4 of rules/00 are met; list anything missing instead of closing.
On confirm: register -> DONE + history + run-log; append reusable learnings to memory/org-facts.md.
