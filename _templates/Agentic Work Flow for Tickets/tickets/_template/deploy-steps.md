# <KEY> — Deploy steps (generated by /ticket-package; EXECUTED BY HUMAN ONLY)
## Pre-deploy checklist
## Commands (fill the org alias; verify with `sf org display --target-org <ALIAS>` first)
```
sf project deploy start --manifest tickets/<KEY>-<slug>/package/package.xml --target-org <ALIAS> --dry-run --test-level RunSpecifiedTests --tests <TestClassNames>
# if clean:
sf project deploy start --manifest tickets/<KEY>-<slug>/package/package.xml --target-org <ALIAS> --test-level RunSpecifiedTests --tests <TestClassNames>
```
## Post-deploy verification
## Rollback note
