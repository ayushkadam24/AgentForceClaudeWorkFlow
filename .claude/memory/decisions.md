# Decision Log (D-###)

Format per entry: ID | date | decider | decision | rationale | alternatives rejected.
Agents append; humans may append; nobody edits past entries (corrections = new entry superseding).

| ID | Date | Decider | Decision | Rationale | Rejected |
|---|---|---|---|---|---|
| D-001 | 2026-07-11 | human | Pipeline workspace lives at the root of the existing SFDX project | one project, one org config, .claude auto-discovered | nested sub-project |
| D-002 | 2026-07-11 | human | PIPELINE_STATE.md YAML block is the single machine-readable state; 4 human gates enforced | auditable, resumable, human-controlled | Jira status as the state spine |
| D-003 | 2026-07-11 | human | 00-inputs/ are immutable; intentional gaps stay in until the retro | the POC is graded on catching them | "cleaning up" inputs |
| D-004 | 2026-07-11 | human | Jira (Atlassian MCP) connection DEFERRED — pm-planner and qa-engineer work file-locally: 02-build/jira-log.md is the ticket source of truth until connected. Jira project exists (currently "Public Health App", key SCRUM — to be renamed/re-keyed to VS before mirroring). | POC starts without external dependency | connecting Jira before kickoff |
| D-005 | 2026-07-11 | client — DHO (Dr. Kulkarni), workshop §2 | Capacity model must support BOTH a daily number and per-session/time-distributed capacity ("design it so both work") | Reconciles the Shinde (daily) vs Pawar (session) contradiction without picking a loser; morning-only booking must be preventable | forcing one facility onto the other's model |
| D-006 | 2026-07-11 | client — DHO (Dr. Kulkarni), workshop §4 | Confirmed bookings get priority; walk-ins are served from remaining capacity; a portion of daily capacity is reserved for walk-ins | Cannot turn away a citizen without a smartphone; walk-ins are permanent reality | booking-only system with no walk-in path |
| D-007 | 2026-07-11 | human (design sign-off, OQ-001) | Capacity is defined PER SESSION: a facility defines 1..n sessions per day; a daily-thinking facility defines ONE session covering the whole day. Slots are generated within sessions. | One model satisfies Shinde (daily) and Pawar (session) with no special cases; operationalizes D-005 as a concrete schema | separate daily-vs-session models; a single daily-number-only model (Pawar's morning-rush objection) |
| D-008 | 2026-07-11 | human (design sign-off, OQ-002) | Slot granularity = 15 minutes, stored as per-service configuration (VS_Setting__mdt or field on the Service), default 15. No UI to change it in the POC. | Configurable without hardcoding; vaccination default agreed; UI out of pilot scope | hardcoded 15 min; per-facility granularity UI in POC |
| D-009 | 2026-07-11 | human (design sign-off, OQ-003) | Walk-in reserve = 25%, single GLOBAL config value in VS_Setting__mdt. Walk-ins consume reserved capacity only. Per-facility overrides are phase 2. | Operationalizes D-006 with a concrete, tunable figure; global keeps POC simple | per-facility reserve % in POC; hardcoded 25% |
| D-010 | 2026-07-11 | human (design sign-off, OQ-004) | Cancel/reschedule cut-off = 4 hours before slot start, GLOBAL config value (VS_Setting__mdt). | Agreed default made tunable per rules/20; global sufficient for pilot | hardcoded 4 h; per-facility cut-off in POC |
| D-011 | 2026-07-11 | human (design sign-off, OQ-020) | Patient identity/de-dup: EXACT match on mobile + full name + DOB = same patient; otherwise a new patient record. One mobile may own many patients (booker ≠ patient). NO fuzzy matching in POC — possible duplicates accepted and logged as a known limitation. | No Aadhaar (REQ-044) so no natural key; exact match is deterministic and defensible; fuzzy matching is scope/risk not warranted in a POC | Aadhaar key (forbidden); fuzzy/soft matching + manual merge (deferred to phase 2) |
| D-012 | 2026-07-11 | human (design sign-off, OQ-025) | Scope = VACCINATION ONLY for F-001 and this POC. Keep the Service object generic (service-type field) so OPD is additive later, but build nothing OPD-specific. | Focuses the pilot; generic Service object avoids precluding OPD (OQ-016/A-002) | OPD in POC scope; vaccination-only object model that blocks OPD later |
| D-013 | 2026-07-11 | human (design sign-off, OQ-027) | Citizen auth = mobile-number + OTP, implemented BEHIND an interface with a stub OTP provider in the POC (fixed test code in the scratch org; real SMS later). No passwords, no Aadhaar, nothing else. | Feature-phone friendly, no Aadhaar, low-literacy; interface lets the real provider swap in without redesign | password auth; Aadhaar/OTP-on-Aadhaar; hardcoding a live OTP gateway into the POC |
| D-014 | 2026-07-11 | human (design sign-off, OQ-011/OQ-012) | SMS vendor/DLT DEFERRED — does not block F-001. All notifications write to VS_Notification_Log__c through the VS_ISmsProvider interface with a log-only implementation. Real gateway swaps in later without design change. | Removes an external dependency from the critical path; interface + log preserves the send surface for later | blocking F-001 on vendor selection; sending real SMS in the POC |
| D-015 | 2026-07-11 | architect | The enforceable §3.4 capacity ceiling and the `SELECT … FOR UPDATE` lock live on `VS_Slot__c` (`VS_Capacity__c`/`VS_Booked_Count__c`), even though capacity is authored on `VS_Session__c` (D-007). Session bookable capacity (75% after the D-009 reserve) is distributed across its 15-min slots at generation time; the 25% walk-in reserve is a session-level counter (`VS_Walk_In_Reserve_Count__c`/`VS_Walk_In_Used_Count__c`) that online slots never draw from. Online booking locks the slot row; walk-in serving locks the session row. | rules/20 mandates FOR UPDATE on the slot row and REQ-008 defines the ceiling as "that slot's published capacity"; a per-slot lock gives row-scoped concurrency (contention only within one slot) so 6,000/day drive peaks (REQ-062) scale across slots/facilities; holding the reserve at session level implements D-009 without leaking reserved capacity into bookable slots | locking the session row for online booking (coarser, more contention); maintaining booked count via roll-up/trigger (does not serialize — forbidden by rules/20) |
| D-016 | 2026-07-11 | architect | Booking reference (`VS_Appointment__c.VS_Booking_Reference__c`) and the future certificate ID are random, non-guessable, human-typeable 8-char Crockford base32 codes, stored as Unique External IDs, generated inside the booking service (`VS_ReferenceGenerator`) | REQ-002 needs a unique typeable reference for feature-phone check-in/lookup (REQ-019/021); REQ-030 needs a non-guessable certificate ID; random + unique-external-id satisfies both and de-duplicates at the DB | sequential/guessable IDs (enumerable — fails REQ-030); using the Salesforce record Id (not typeable; leaks org info) |
| D-017 | 2026-07-11 | architect | Patient exact-match de-dup (D-011) is enforced by a Unique External ID `VS_Patient__c.VS_Match_Key__c` = `normalize(full name)|DOB|mobile`, set by `VS_PatientService.findOrCreate()` using upsert-by-external-id | Makes D-011 deterministic and race-safe at the database (a duplicate insert fails the unique constraint instead of relying on a non-locked SOQL existence check); no Aadhaar natural key exists (REQ-044) | service-layer SOQL existence check without a DB constraint (races under concurrency); fuzzy/soft matching (explicitly rejected by D-011) |
| D-018 | 2026-07-11 | architect | A session flagged `VS_Session__c.VS_Is_Drive_Day__c` overrides a `VS_Holiday__c` closure for that facility+date only; slot generation skips holiday dates unless such a drive session exists (operationalizes OQ-019) | Staff must add capacity on otherwise-closed days for special Sunday drives (REQ-012, WS §3) without editing the shared holiday calendar; scoped to facility+date avoids blanket overrides | a global holiday-suppression switch; deleting holiday rows to open a drive (loses the calendar) |
| D-019 | 2026-07-11 | human (design sign-off) — **SUPERSEDES D-015** | ONE way to consume capacity: a single booking service method locks the SESSION row (`VS_Session__c`) with `SELECT … FOR UPDATE` for EVERY booking type — online, staff, and walk-in — then checks slot capacity and the walk-in reserve inside that single lock, then inserts the appointment. No second lock target; no per-path lock. §3.4 overbooking is provably impossible with one serialized code path. EP-03 still routes to dev-senior and still carries capacity-exhaustion + parallel-booking tests, now against one path. | Two lock rows for two booking paths (D-015) is unproven and creates a seam where online + walk-in can race on the same session capacity; serializing all bookings within a session is harmless at pilot volumes (~120/day/facility) and makes correctness trivially auditable with one lock instead of two | D-015 dual lock target (slot row online / session row walk-in) — retracted as unproven; a lock-free roll-up count (does not serialize) |
| D-020 | 2026-07-11 | architect (propagating D-019) | Under the single `VS_Session__c` FOR UPDATE lock (D-019), the per-slot published ceiling REQ-008 requires ("that slot's published capacity") is RETAINED on `VS_Slot__c` (`VS_Capacity__c`/`VS_Booked_Count__c`), but those counters are read and written ONLY inside the parent session's lock. `VS_Session__c` is the sole lock target and the source of truth for the walk-in reserve pool (`VS_Walk_In_Used_Count__c`/`VS_Walk_In_Reserve_Count__c`). No slot-row lock exists; no roll-up/trigger maintains the count. `VS_BookingService.book` takes a `channel` arg so online/staff draw slot bookable capacity and walk-ins draw the session reserve, all inside the one lock. No object/field added, removed, or renamed vs the pre-D-019 model. | REQ-008 still demands a per-slot ceiling, so the slot count must survive; keeping it read/written only under the session lock makes it race-free without a second lock (satisfies D-019); A-005 (sum of slot capacities == bookable capacity) makes per-slot enforcement under the session lock equivalent to enforcing the session bookable ceiling | dropping the slot booked count and tracking only a session-level count (cannot enforce REQ-008 per-slot ceiling); a second lock on the slot row (retracted by D-019); roll-up summary count (does not serialize — rules/20) |
| D-021 | 2026-07-11 | pm-planner | Sprint plan uses 4 sprints, not 5: Sprint 4 combines the EP-07 notification seam with the EP-08 security/sharing/retention/seed-data hardening remainder, since both are individually light (each under a full week of either dev track) and neither depends on the other's completion internally. | Avoids two artificially under-loaded sprints; EP-07/EP-08 remainder items are cross-cutting hardening that can proceed in parallel across dev-mid and dev-senior without contention | a separate 5th sprint for EP-08 alone (would leave both final sprints under a week's load); merging EP-07 into Sprint 3 instead (would overload dev-senior in that sprint alongside the citizen booking journey, VS-16) |
| D-022 | 2026-07-11 | pm-planner | REQ-036 (bulk export restricted to District MIS + logged) is ticketed ONLY as permission-set-level gating (folded into VS-04). No ticket is created for an actual export mechanism/UI. | Technical design explicitly scopes REQ-036 as "role gating built; export UI deferred" (§7.1); dashboards/export UI are named Deferred by the human scope guardrail at sign-off — inventing an export-mechanism ticket would exceed EP-01..08 scope | a dedicated VS-## ticket building an export button/report (would invent scope not in any epic; flagged instead in sprint-plan.md "Needs architect" §5) |
| D-023 | 2026-07-11 | human (pre-build sign-off; formalizes A-005) | POC ships EVEN slot-capacity distribution. The distribution logic MUST live in exactly ONE private method of the slot-generation service (VS-06) so that swapping to weighted/front-loaded distribution later is a single-method change, not a redesign. **DRIFT-CHECK RADAR:** BA_ARCH_CONFIRM must confirm distribution stayed isolated in one private method. | Even split is sufficient for the pilot; isolating it behind one method keeps the weighted-distribution seam trivial (A-005 owner: MO Shinde/Pawar via DHO) | weighted/front-loaded distribution in the POC (unrequested complexity); inlining the split across slot-gen (would force a redesign later) |
| D-024 | 2026-07-11 | human (pre-build sign-off; addresses OQ-013) | DPDP consent + notice wording is built with clearly-marked PLACEHOLDER copy in a Custom Label, prefixed `[[DRAFT — pending department approval]]`. Approved wording later is a Custom Label edit only — zero code change. Blocks LAUNCH, not build. Tracked on the open-questions.md Launch Checklist. **DRIFT-CHECK RADAR:** BA_ARCH_CONFIRM must confirm consent copy is a Custom Label with the DRAFT prefix, not hardcoded. | Lets registration build proceed without waiting on department legal approval (OQ-013, 1–3 wk lead); Custom Label swap is trivial and auditable | hardcoding consent text (forces a code change + redeploy on approval); blocking the build on department sign-off |
| D-025 | 2026-07-11 | human (pre-build) — **adjusts rules/20 "scratch org ONLY" deploy target** | The POC target org is a free **Salesforce Developer Edition** org (persistent, single fixed org), NOT a scratch org. Deploy/verify uses `sf org login web` + `sf project deploy start` against that DE org; `sf org create scratch` and `config/project-scratch-def.json` do not apply. All other rules/20 standards (VS_ naming, WITH USER_MODE, FOR UPDATE, ≥85% coverage, dry-run before claiming buildable) stay in force. Metadata must stay DE-compatible: no scratch-only feature dependencies; Experience Cloud (VS-14/16) must fit DE's included community licenses (verify license availability at that ticket); watch DE storage limits for seed data (VS-22 already idempotent). Persistent org ⇒ seed/purge scripts MUST be reset-safe (no assuming a clean org). | User is doing hands-on Agentforce/Data Cloud learning on a free DE org; DE is persistent and zero-cost but has fixed limits and no scratch feature-toggles | scratch org (assumed by scaffolding, but user isn't using one); sandbox (needs a paid org) |
| D-026 | 2026-07-12 | human (fresh-session deploy gate) | **Sprint-1 deploys in TWO phases** to resolve the `checkOnly` `UNKNOWN_EXCEPTION` from formula fields reading `$CustomMetadata` records created in the same transaction. **Phase 1:** `VS_Setting__mdt` (type + 6 records) + all components NOT referencing it (real deploy, commits the CMDT records first). **Phase 2:** `VS_Session__c` (and any `$CustomMetadata`-reading formula fields — `VS_Walk_In_Reserve_Count__c`, `VS_Bookable_Capacity__c`) + remaining dependents, deployed once the CMDT exists in-org. Each phase gets its own clean dry-run before its real deploy. Prereq: deploy blockers 1–3 (VS_Appointment__c/VS_Patient__c object descriptions >1000, VS-03 flow XSD element order) fixed first. **DRIFT-CHECK RADAR:** phase split is a deploy-sequencing decision only — zero change to design, field definitions, or the §3.4 lock. | Deterministic and the standard SF pattern for CMDT-formula dependencies; `checkOnly` cannot commit CMDT records before formula compilation, so single-transaction validation can't be made reliable — splitting the transaction removes the ambiguity while preserving per-phase dry-run validation | single real (non-checkOnly) deploy test (unproven — may still fail on same-transaction CMDT compilation, costs a round trip); leaving VS_Session__c formulas as Apex reads (larger redesign, unrequested) |
| D-026a | 2026-07-12 | human (fresh-session, refines D-026 after devops bisection) | **Root-cause correction + Phase-1 mechanic.** D-026's original premise (the `VS_Session__c` `$CustomMetadata` formulas were the sole blocker) was INCOMPLETE. devops bisection (Deploy IDs `0AfgL00000QxbfhSAB`/`0AfgL00000QxbpNSAR`) proved a lower-layer cause: on an org that has NEVER had `VS_Setting__mdt` committed, a first-time CMDT **type+records** bundle is **structurally un-dry-runnable** — `checkOnly` validates the 6 records against a type created in the same transaction that it never commits → opaque `UNKNOWN_EXCEPTION`. Type+fields ALONE dry-runs clean; adding one record fails. **Resolution (human-approved): Phase 1 splits into 1a + 1b.** 1a = real-deploy the CMDT type + 2 fields (dry-run-proven clean first). 1b = dry-run the 6 records against the now-committed type (validatable because the type exists), then real-deploy them. Phase 2 (rest of build incl. VS_Session__c `$CustomMetadata` formulas) then dry-runs against committed records as originally designed. This preserves dry-run discipline everywhere it is technically possible; the one unavoidable relaxation is that 1a's real deploy precedes any records — but 1a itself is dry-run-clean. Two-phase strategy of D-026 otherwise stands. | The dry-run gate is provably unsatisfiable for first-time CMDT records; type-first makes the records dry-run-validatable, minimizing the ungated write to just the proven-clean type deploy | single combined real Phase-1 deploy of type+records with NO dry-run on the records (devops-recommended, faster, but leaves the 6 records un-dry-run-validated — human chose the more disciplined split); holding the deploy (no progress) |
| D-026b | 2026-07-12 | human (fresh-session, refines D-026a after Phase-1a landed) | **1b's premise disproved.** Phase 1a real-deployed clean (Deploy `0AfgL00000QxRmoSAF` — `VS_Setting__mdt` type + `VS_Value__c`/`VS_Value_Text__c` now committed). But the follow-up dry-run of the 6 `VS_Setting.*` records STILL failed opaquely (`UNKNOWN_EXCEPTION` -315522575, Deploy `0AfgL00000QxeSHSAZ`) EVEN WITH the type committed — so on this DE org, CustomMetadata **records** cannot be `checkOnly`-validated at all, independent of type presence (not the same-transaction limitation D-026a assumed). **Resolution (human-approved): land the 6 records via a REAL (non-checkOnly) deploy** — routine for CMDT records, same pattern as the approved 1a. After records commit, Phase 2 proceeds (dry-run + real, RunLocalTests). No source defect; deploy-mode limitation only. | The records' dry-run gate is provably unsatisfiable on this org regardless of ordering; a real CMDT-record deploy is standard SF and the only way to land config; 6 static config rows = low risk | holding at Phase 1a (blocks all downstream — slot-gen, booking §3.4 — indefinitely on an un-closable dry-run gate) |
| D-027 | 2026-07-12 | devops (org-capability finding; ESCALATED to human) | **This DE org rejects ALL CustomMetadata RECORD deploys via the Metadata API** with an opaque `UNKNOWN_EXCEPTION (-315522575)`, in EVERY mode tested: `checkOnly` dry-run (`0AfgL00000QxeSHSAZ`) AND real deploy (`0AfgL00000QxclSSAR`); 6-record batch AND a single record (`0AfgL00000QxYLGSA3`); AND a brand-new minimal nil-free diagnostic record with a unique DeveloperName (`0AfgL00000QxbnmSAB`). Meanwhile the `VS_Setting__mdt` TYPE + fields deployed clean (Phase 1a, `0AfgL00000QxRmoSAF`) and are queryable (anon Apex SOQL compiled, count=0), and ordinary objects deploy fine. So this is NEITHER a source defect (a fresh minimal record fails identically) NOR a deploy-mode issue (supersedes the D-026a/D-026b hypotheses) — it is an org-side platform limitation of this specific Developer Edition org. **Resolution (pending human action): create the 6 `VS_Setting.*` records MANUALLY via Setup -> Custom Metadata Types -> VS Setting -> Manage Records -> New** (WalkInReservePct=25, CutOffHours=4, DefaultSlotGranularityMins=15, BookingHorizonDays=14, NoShowThresholdCount=3, ReminderOffsetsHours text "24,3"), then a drift-check retrieve records them as the source of truth. Phase 2 (VS_Session__c `$CustomMetadata` formulas + the §3.4 booking suite) is BLOCKED until at least the WalkInReservePct record exists in-org. | MDAPI record deploy is provably non-functional on this org across all modes and inputs; manual UI creation is the standard SF fallback and unblocks all downstream; 6 static config rows are trivial to hand-enter | continuing to retry MDAPI record deploys (protocol: the input must change — and every changed input still fails identically); editing the valid record source (source is correct, would not help); abandoning CMDT config (rejected — rules/20 mandates Custom Metadata for tunables) |
| D-028 | 2026-07-12 | human (fresh-session deploy gate) | **Sprint-1 real deploy runs `--test-level RunSpecifiedTests` naming the 21 passing tests; the 3 walk-in tests are a documented org test-execution limitation, NOT a defect.** This DE org abnormally FLS-filters even SYSTEM-MODE Apex DML during deploy-time `RunLocalTests`, so `VS_BookingService.book()`'s system-mode `update session` on the REQUIRED counter `VS_Walk_In_Used_Count__c` (D-020) fails "fields inaccessible" — a required field cannot be granted field-FLS (VS-04 trap), so the 3 walk-in tests (`testHappyPath_walkInBooking`, `testWalkInReserveExhaustion_neverOverbooks`, `testMixedChannels_sameSession_noOverbooking`) cannot execute here. Verified real-org results at deploy time: 21/24 pass, ~95% new-class coverage (VS_BookingService 94 / VS_ReferenceGenerator 100 / VS_SlotGenBatch 95 / VS_SlotGenerationService 95), and §3.4 ONLINE no-overbooking + even-distribution + 250-session governor (SOQL≤4/DML==1) + drive-day override + idempotency + reference-collision all PASS. **Walk-in overbooking (§3.4 walk-in/mixed-channel) is routed to QA's parallel LOAD test** — always the real §3.4 concurrency proof per the VS-09 packet (unit tests run one transaction, can't prove FOR UPDATE serialization). The two BLOCKER-G code changes (CMDT reads via `getInstance()`; collision detect via `StatusCode.DUPLICATE_VALUE`) are genuine production hardening kept regardless. | The code + metadata are production-correct and exceed the coverage bar; the only gap is an org-specific test-execution quirk on intentionally-system-mode code that is safe at real runtime; RunSpecifiedTests lands the code with full coverage evidence while honestly excluding the un-executable 3, and QA's load test is the correct place to prove walk-in concurrency anyway | NoTestRun (loses on-deploy test-gate evidence); weakening book()'s system-mode counter design or adding FLS to a required field to force the tests green (would corrupt the design to satisfy an org quirk — rejected); holding the deploy (code is ready, no benefit) |
| D-028a | 2026-07-12 | orchestrator (mechanism of D-028, after deploy-API finding) | **MECHANISM: the 3 walk-in tests are physically split into their own class `VS_BookingServiceWalkInTest`; the deploy runs class-level `RunSpecifiedTests VS_BookingServiceTest,VS_SlotGenBatchTest`.** The Metadata *deploy* API's RunSpecifiedTests accepts CLASS names only — method-level `Class.method` exclusion silently runs 0 tests (real-deploy `0AfgL00000QyQv7SAF` rolled back on 0% coverage). So D-028's "21 passing tests" is realised by moving the 3 un-executable walk-in methods out of VS_BookingServiceTest into a separate class that stays in source (deployed, NOT named in the run) for QA's load test. Zero change to D-028's intent or to production code. | Class-level is the only granularity the deploy API honours; a self-contained walk-in test class keeps the 21 runnable methods green on deploy while preserving the walk-in tests in source for QA | method-level --tests (unsupported by deploy API — proven); deleting the walk-in tests (loses QA's load-test scaffolding); NoTestRun (D-028 rejected) |
| D-029 | 2026-07-12 | architect (focused post-drift ruling; requested by human) | **§3.4 walk-in counter-persist DML is made robust by updating a FRESH minimal sObject carrying ONLY Id + the mutated counter(s) — NOT the loaded lock-query record. IMPLEMENTATION-ROBUSTNESS, PRESERVES the design (D-019/D-020), NOT a deviation.** Runtime finding (QA TC-002 ×3, load harness `0AfgL00000Qz29BSAR`): `VS_BookingService.book()`'s system-mode `update session;` (walk-in counter `VS_Walk_In_Used_Count__c` persist) throws `DmlException: fields being inaccessible on VS_Session__c` because the `session` sObject was loaded by the FOR UPDATE query CARRYING the read-only `$CustomMetadata` FORMULA field `VS_Walk_In_Reserve_Count__c`, which this DE org's anomalous runtime FLS-on-system-mode-DML (D-028 extends to runtime) treats as inaccessible for the admin context. **SANCTIONED FIX = option (a):** replace `update session;` with `update new VS_Session__c(Id=session.Id, VS_Walk_In_Used_Count__c=<used+1 computed under the lock>);` — a fresh sObject that does NOT carry the FLS-hidden formula field, so the system-mode DML no longer references an inaccessible field. Same treatment applied to the online branch for symmetry/defense-in-depth: `update new VS_Slot__c(Id=slot.Id, VS_Booked_Count__c=<booked+1>, VS_Status__c=<Open/Full>);`. **Option (b) REJECTED** — `Database.update(session, AccessLevel.SYSTEM_MODE)` is semantically identical to the plain `update session;` that already fails (both system-mode); it does not address the root cause (the inaccessible formula field carried on the in-memory sObject), so it would not fix the anomaly. INVARIANTS PRESERVED (must not break): (1) the single `SELECT … FOR UPDATE` on `VS_Session__c` still taken and STILL loads `VS_Walk_In_Reserve_Count__c`/`VS_Walk_In_Used_Count__c` for the reserve check — only the UPDATE DML shape changes, not the lock SELECT; (2) both channels stay one method / one lock / one write path; (3) counter incremented exactly once, computed from the value read UNDER the lock (`used+1`) and carried on the fresh sObject — NO re-query (a second SOQL would break serialization/introduce TOCTOU); (4) appointment insert stays `insert as user` (USER_MODE); (5) NO roll-up/formula/trigger/flow introduced to maintain counters (D-020); (6) persistence stays system-mode (the service is the sole trusted counter owner); (7) `VS_Walk_In_Used_Count__c` stays REQUIRED — the fix works precisely because required fields are always accessible, so NO FLS is granted to it and it is NOT made optional (VS-04 trap avoided). RE-VERIFICATION BAR (QA must re-demonstrate for §3.4 walk-in PROVEN): re-run TC-002 (N≥20 concurrent last-place walk-in burst, reserve=1, ×3) → exactly 1 `success:true`, `walkInUsed=1 ≤ reserve=1`, every other call coded `WALKIN_RESERVE_FULL` (not `DmlException`); re-run TC-003 mixed (×3) → exactly 1 online + exactly 1 walk-in success, pools disjoint (`slotBooked≤cap`, `walkInUsed≤reserve`); regression re-run TC-001 online (×3) since the slot branch is also touched → still exactly 1 success, no overbooking; zero rows above capacity on all repeats (any double-book = Sev-1). GO for dev-senior to implement in `VS_BookingService.book()`; VS_BookingServiceWalkInTest may become deploy-time-executable after the fix (dev/devops re-check; not required for GO). | The failure is an org-specific runtime anomaly on intentionally-system-mode code, not a design flaw; narrowing the DML to a fresh Id+counter sObject removes the accidental inaccessible-formula-field reference while keeping every §3.4 crown-jewel invariant (one lock, one path, system-mode counter, no roll-up) exactly as designed — the cheapest change that closes the whole failure class; applying it to both branches gives a symmetric write path and re-verification of online costs nothing since the load test re-runs all three TCs anyway | (b) `Database.update(...,AccessLevel.SYSTEM_MODE)` — identical mode to the failing plain update, does not remove the inaccessible field, would not fix it; re-querying a lean session/slot before the update (adds a SOQL inside the lock — unnecessary and risks serialization/TOCTOU reasoning; the in-memory computed value is authoritative under the lock); granting FLS to / de-requiring `VS_Walk_In_Used_Count__c` (impossible for a required field + corrupts the model to satisfy an org quirk — same rejection as D-028); leaving walk-in unproven and shipping online-only (fails RFP §3.4 for the walk-in channel — D-006/D-009 make walk-in a permanent reality) |
| D-030 | 2026-07-13 | architect (post-DONE forward-build design ruling; requested by human) | **ACCEPT-WITH-AMENDMENT — an OTP-verified citizen books through the ONE §3.4 service via a single NARROW elevation, NOT by granting citizens edit on capacity rows (D-020 red line held).** (1) SHARING PRIMITIVE: change `VS_BookingService` from `with sharing` to `inherited sharing` — ACCEPTED. `inherited sharing` is secure-default at the top of the stack (runs `with sharing` when it is the entry point) and otherwise takes the caller's mode, so every existing caller (VS_BookingControllerTest `with sharing`; the 3 service test classes are `@isTest private` inheriting their runAs/caller context; internal service callers) keeps its present behaviour and ONLY the portal wrapper elevates. It is the only construct that lets ONE `book()` body serve both a `with sharing` internal caller AND a `without sharing` portal caller WITHOUT forking the crown-jewel logic — forking book() would re-open the exact online/walk-in seam D-019 closed (forbidden). A dedicated permanently-`without sharing` booking method collapses to the same thing (it must still delegate to the single book(), which must therefore be `inherited sharing`), so a separate method buys nothing and adds a second entry point to audit — rejected in favour of inherited sharing + one wrapper. (2) ELEVATION: the portal path is invoked from EXACTLY ONE thin `without sharing` wrapper adjacent to the C9 gate (accept the candidate's inner `without sharing` class in `VS_BookingController`, e.g. `PortalBooking`; an inner `without sharing` class of the service is an acceptable alternative). It MUST be the SOLE `without sharing` caller of book()/cancel()/reschedule() and clearly named; any future `without sharing` caller of the service is a security-review item (code-review invariant + class comment required). The WHOLE book() body must run elevated, not just the DML: `SELECT … FOR UPDATE` itself requires edit access to lock the row, so a least-privilege citizen cannot even acquire the lock under `with sharing`; a "DML-only" elevation is INSUFFICIENT. The appointment insert stays `insert as user` (FLS/CRUD enforced on the citizen's own record). (3) C9 SERVER-SIDE GATE (MANDATORY, currently MISSING): `VS_BookingController.book(...)` MUST, before calling the service, re-query a `VS_OTP_Verification__c` challenge for the mobile with `VS_Verified__c=true` and not expired, and reject otherwise with a coded, user-actionable error. Today the controller only CLAIMS this in ApexDoc (lines ~77-78) — the code at lines 85-94 does NOT re-check → MUST-FIX. The gate SOQL runs inside the elevated context so citizens need NO read on OTP records; never trust a client-side verified flag; consume/expire the challenge on use to block replay. (4) §3.4 INVARIANTS CONFIRMED PRESERVED (sharing changes record VISIBILITY, not lock/counting): single `SELECT … FROM VS_Session__c … FOR UPDATE` lock target and pessimistic semantics UNCHANGED; single-lock discipline (D-019/D-020) UNCHANGED; disjoint online-slot vs walk-in-reserve pools UNCHANGED; D-029 fresh-minimal-sObject counter writes UNCHANGED; counter computed once under the lock with NO re-query (no TOCTOU) UNCHANGED; `insert as user` UNCHANGED. `without sharing` merely lets the citizen's execution context ACQUIRE the existing lock it could not hold under `with sharing`. (5) CANCEL/RESCHEDULE via the SAME elevated path is a DISTINCT security case and a MUST-FIX condition on any portal cancel/reschedule: running `without sharing` removes the record-ownership check that `with sharing` gave `loadCancellableAppointment()`, so the controller MUST enforce explicit ownership (OTP-verified mobile == `VS_Appointment__c.VS_Booked_By_Mobile__c`) BEFORE elevating — else IDOR (a citizen cancels another citizen's booking by Id). book() has no IDOR surface (it creates the citizen's own new record). SHIP BAR: VS_BookingServiceTest + VS_BookingServiceWalkInTest + VS_BookingServiceCancelRescheduleTest + VS_BookingControllerTest all stay GREEN; a NEW test proves book() succeeds under `runAs` a user holding ONLY the least-privilege portal permset (NO Session/Slot edit — harness permset NOT assigned) and increments the counter, plus the C9-gate reject/accept pair; re-run the D-029 §3.4 load bar (TC-001 online ×3 / TC-002 walk-in last-place burst ×3 / TC-003 mixed ×3, zero overbooking) THROUGH the portal `without sharing` path as the least-privilege citizen context; metadata-lint clean (only the 2 known D-026 $CustomMetadata FAILs); validate-only deploy RunLocalTests green. PORTAL PERMSET SHAPE (D-020): MUST have — Read on Facility/Service/Facility_Service/Session/Slot (availability); Create+Read on VS_Appointment__c + FLS-Edit only on the fields book() sets via `insert as user`; Create/Read on VS_Patient__c + FLS on C1 fields + VS_Match_Key__c for findOrCreate. MUST NOT have — ANY Edit (object or FLS) on VS_Session__c or VS_Slot__c (incl. VS_Booked_Count__c / VS_Walk_In_Used_Count__c / VS_Status__c on those rows) [the D-020 red line]; the VS_Booking_Engine_Test_Context permset (TEST-ONLY, never on a real/guest user); Delete / View-All / Modify-All / VS_Bulk_Export. GO for a developer to implement in VS_BookingController + VS_BookingService + the portal permset; architect does not write the Apex. | The only reason booking works today is the TEST-ONLY VS_Booking_Engine_Test_Context granting the harness user edit on capacity rows, which its own description says does NOT resolve A-018 and which would violate D-020 + least-privilege if given to a citizen; guest sharing is read-only so a guest can never hold write-sharing on Session/Slot. Elevating the single existing book() path via inherited-sharing + one named wrapper closes A-018 for the portal with the smallest privilege surface, keeps ONE crown-jewel code path (no fork = no new §3.4 seam), and touches record VISIBILITY only — every D-019/D-020/D-029 invariant is untouched. The C9 gate belongs on the server because a client flag is forgeable (Annexure C9). | grant citizen/guest edit on capacity rows (violates D-020 + least-privilege; impossible for a pure guest anyway — guest sharing is read-only); keep VS_BookingService `with sharing` and elevate only the counter DML (insufficient — the SELECT…FOR UPDATE lock read itself needs edit/elevation); a dedicated permanently-`without sharing` booking method (still must delegate to the one book() → collapses to inherited sharing + a wrapper, adds a 2nd audited entry point for nothing); bare no-sharing keyword on the service (insecure entry-point default vs inherited sharing's secure default); trusting a client-side "verified" flag (forgeable — fails C9); blanket-elevating cancel/reschedule without an explicit mobile-ownership check (opens IDOR) |
| D-030a | 2026-07-13 | architect (amends D-030 after implementation finding; requested by coordinator) | **D-030 stays GO — the design is correct; the least-privilege portal booking SUCCESS is a RUNTIME proof on a STANDARD org, NOT a deploy-time unit test, exactly as §3.4 walk-in was handled (D-028/D-029).** IMPLEMENTATION FINDING: under `runAs` a user holding ONLY VS_Booking_Portal (no Slot/Session edit, per D-020), book() CLEARS the SHARING barrier (acquires the §3.4 VS_Session__c FOR UPDATE lock, passes FLS, and the `insert as user` appointment succeeds — the OTP-consume UPDATE and Patient upsert also succeed) and THEN throws `System.TypeException: UPDATE not allowed on VS_Slot__c` at the counter DML — an OBJECT-CRUD wall, because this DE org's D-028 anomaly enforces the ACTING USER's object CRUD (and FLS, D-029) even on system-mode DML, which `without sharing` does NOT bypass (sharing is not CRUD). So D-030's premise "sharing was the only blocker" HOLDS on a STANDARD org (plain system-mode DML ignores CRUD/FLS → the least-privilege citizen books fine) but not on THIS DE org. RULINGS: (1) DO NOT grant Slot/Session edit to VS_Booking_Portal — that is the D-020 red line, and on this org it would ALSO require FLS-edit on the exact counter fields (VS_Booked_Count__c / VS_Walk_In_Used_Count__c / VS_Status__c), letting a citizen directly corrupt counters via API = full breach; REJECTED even as a documented deviation. (2) DO NOT rely on `Database.update(rec, AccessLevel.SYSTEM_MODE)` as the fix — it is semantically identical to the plain system-mode update that already fails here (same rejection as D-029 option b); making the system-mode intent explicit in code is harmless but changes nothing on this org and must NOT be presented as resolving the anomaly. (3) No async / trigger / platform-event counter path — it breaks the single-transaction FOR UPDATE lock and D-020's no-rollup rule; REJECTED. CONCLUSION: on this org there is NO in-transaction way to complete the counter DML for a Slot-edit-less user short of the D-020 breach; the design is correct for a standard org and the DE-org limitation is the SAME class as §3.4 walk-in and the D-025 "reconfirm org quirks on a non-DE org" caveat already accepted at release. SHIP-BAR AMENDMENT (supersedes the D-030 "least-priv-runAs booking test increments the counter" item): the A-018 least-privilege booking SUCCESS is proven at RUNTIME on a standard org / standard-behaving sandbox (or a runtime REST/site call) as a QA Tier-1 launch precondition — NOT a deploy-time unit test. A-018 PROOF TEST (honest + green on this DE org): under `runAs` VS_Booking_Portal ONLY, assert book() THROWS with the message containing "UPDATE not allowed" on VS_Slot__c / VS_Session__c — this proves the `without sharing` elevation removed the SHARING blocker (the call reached the counter DML past the lock, FLS, and the `insert as user` appointment) and the SOLE residual is the documented D-028 CRUD anomaly, not a design defect. Atomicity (no committed appointment) is guaranteed by transaction-end rollback on the uncaught throw and is proven at runtime — do NOT assert a post-catch appointment count (catching the exception suppresses the rollback and would read a false 1). Keep the harness-permset (VS_Booking_Engine_Test_Context) in-transaction success test as coverage, EXPLICITLY labelled harness-only / NOT a least-privilege proof. The other three named regression tests + C9 gate tests stay as D-030. | The failure is an org-specific CRUD-enforcement anomaly (D-028) on intentionally-system-mode code, not a design flaw; every §3.4 invariant (D-019/D-020/D-029) is intact and the citizen provably cleared sharing + FLS + insert-as-user; granting counter-row edit to satisfy an org quirk would corrupt the least-privilege model (the exact D-020/D-028 trap already rejected in D-028/D-029); a runtime success gate plus a wall-asserting unit test is honest and mirrors the accepted §3.4 walk-in and D-025 handling | grant Slot/Session (or counter-field) edit to the portal permset (D-020 breach — citizens could corrupt counters); `Database.update(..., AccessLevel.SYSTEM_MODE)` as the fix (identical to the failing plain update — D-029 option b); async/trigger/platform-event counter maintenance (breaks the FOR UPDATE single-transaction lock + D-020 no-rollup); asserting a deploy-time SUCCESS path this org cannot execute (dishonest — same reason §3.4 walk-in went to a runtime gate) |
