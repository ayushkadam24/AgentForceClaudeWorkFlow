# Assumptions Register (A-###)

Anything treated as true without client confirmation. Owner = who must verify.
| ID | Date | Raised by | Assumption | Impact if wrong | Owner | Status |
|---|---|---|---|---|---|---|
| A-001 | 2026-07-11 | human | 60/40 smartphone/feature-phone split (flagged ⚠ unverified in personas doc) | channel priorities shift toward SMS | BA → DHO | Open |
| A-002 | 2026-07-11 | ba-analyst | Vaccination is the pilot/primary scope; OPD appointment booking is a configurable service type deferred beyond F-001 (RFP scope is vaccination-centric but current-state §B describes OPD) | If OPD is in-scope now, slot model + effort estimates grow | DHO/Deshmukh (OQ-025) | Open |
| A-003 | 2026-07-11 | ba-analyst | All unresolved tunables (slot granularity, walk-in %, cut-off hours, reminder offsets, booking horizon, no-show thresholds) are implemented as configurable settings (Custom Metadata), so the open numbers do not block build | If a value must be hardcoded/behaviour-shaping, rework needed when confirmed | BA/architect (OQ-002/003/004/005/006/007) | Open |
| A-004 | 2026-07-11 | ba-analyst | Citizen self-service authentication is OTP to the registered mobile (RFP is silent; no Aadhaar; feature-phone friendly) | If a different auth is mandated (e.g., state SSO), portal access redesign | Deshmukh (OQ-027) | Open |
| A-005 | 2026-07-11 | architect | Session bookable capacity is distributed across its 15-min slots by an even split with the remainder assigned to the earliest slots, so that sum(VS_Slot__c.VS_Capacity__c) == VS_Session__c.VS_Bookable_Capacity__c exactly | If MOs expect front-loaded/weighted distribution (e.g. more capacity early morning), the slot-generation algorithm changes | MO Shinde/Pawar via DHO (OQ-001 family) | Open |
| A-006 | 2026-07-11 | architect | Facility-scoped visibility (REQ-053) is implemented with OWD Private + criteria-based sharing to per-facility public groups for staff and Experience Cloud sharing sets (keyed on the citizen's verified mobile) for citizens; per-record read attribution (REQ-054) beyond Login History/field history assumes Salesforce Shield Event Monitoring is available in the target org, else it is accepted as a known POC limitation | If Shield is unavailable, REQ-054 read-level audit is only partially met in the POC | architect → DHO/Deshmukh | Open |
| A-007 | 2026-07-11 | pm-planner | Per-facility public group membership (staff↔facility association, VS-20) is maintained as a manual admin step in the POC — no ticket automates syncing a facility-staff user's facility to their public group membership | If DHS expects automatic membership sync (e.g., on user provisioning), an additional Flow/Apex enabler ticket is needed beyond EP-08's current scope | architect/admin — confirm before broader rollout past the pilot | Open |
| A-008 | 2026-07-11 | dev-mid (VS-01) | `VS_Facility_Service__c.VS_External_Id__c`'s composite-uniqueness AC (design §2.3, VS-01 AC3) is met with a declarative "Default Value" formula (`VS_Facility__c & "-" & VS_Service__c`) on a plain Text field, rather than Apex/Flow automation, to stay declarative-first per rules/20. Assumed this default-value-formula pattern (referencing sibling lookup fields on the same record at insert time) reliably populates on both UI and API/bulk inserts in a Developer Edition org. | If the formula does not populate reliably on API/bulk insert (a known edge case for default-value formulas referencing other fields), the AC3 unique-constraint test will not actually catch duplicates and a small Flow/Apex backfill would be needed — this would be a re-route candidate if it requires more than trivial declarative fix | dev-mid/dev-senior — verify at `sf project deploy start --dry-run` and with a manual duplicate-insert test in the DE org before BA_ARCH_CONFIRM | Open |

**A-005 amendment (2026-07-11, architect):** Still holds under D-019/D-020. The even-split guarantee `sum(VS_Slot__c.VS_Capacity__c) == VS_Session__c.VS_Bookable_Capacity__c` now additionally underpins §3.4 correctness: because per-slot ceilings are enforced inside the single session lock and their capacities sum to the session bookable capacity, enforcing each slot's ceiling is equivalent to enforcing the session bookable ceiling (no path can exceed either). Distribution algorithm unchanged; owner/verification unchanged.

**A-005 amendment (2026-07-11, human/orchestrator):** Formalized as decision **D-023** — POC ships even distribution; the distribution logic must live in exactly ONE private method of the slot-generation service (VS-06) so weighted distribution later is a method swap, not a redesign. Flagged for the BA_ARCH_CONFIRM drift-check to confirm it stayed isolated in one method.

| A-016 | 2026-07-12 | dev-senior (VS-09) | Mixed-channel "shared place" semantics: VS-09's AC says "one place shared across channels, exactly one booking of any type succeeds", but the design default D-020 makes online (slot bookable count) and walk-in (session reserve pool) DISJOINT capacity pools guarded by the SAME session lock. Implemented the design default: the mixed-burst test seeds one place PER pool so exactly one online + one walk-in succeed, each rejected beyond its own ceiling, with no cross-channel consumption. | If DHS actually means a single shared last place drawn down by either channel, the DATA MODEL (not just book()) must merge the two counters — a design change, not a code tweak. Until confirmed, the disjoint-pool reading (D-020) stands and no channel can overbook its own pool or consume the other's. | architect/DHO — confirm at BA_ARCH_CONFIRM | Open |
| A-017 | 2026-07-12 | dev-senior (VS-09) | `book()`'s `bookedById` argument has no storage field in the VS-08 `VS_Appointment__c` schema (no "Booked By User" lookup). It is kept in the signature per the design contract (VS-16/staff attribution) but is currently unused for storage; audit attribution falls to `CreatedById`. | If per-booking user attribution beyond CreatedBy is required (e.g. staff booked on citizen's behalf must be queryable), add a User lookup field to VS_Appointment__c (schema change, VS-08 owner) and set it in book(). | architect/dev — confirm before VS-16 staff-console work | Open |
| A-009 | 2026-07-11 | dev-mid (VS-02) | `VS_Setting__mdt` seed records `BookingHorizonDays`=14, `NoShowThresholdCount`=3, and `ReminderOffsetsHours`="24,3" (via `Value_Text__c`) use the BA's *suggested defaults* from `open-questions.md` (OQ-006, OQ-007, OQ-005 respectively) — unlike `CutOffHours`/`WalkInReservePct`/`DefaultSlotGranularityMins` (D-010/D-009/D-008, human-signed), these three OQs remain status **Open** with no D-### decision ratifying the number. Seeded anyway per A-003 ("open tunables do not block build — implemented as configurable settings") so VS-02 is not blocked. | If DHO ratifies different values (different reminder cadence, no-show threshold, or booking horizon), only a Custom Metadata record edit is needed (no code/redeploy) — but any later ticket that hardcodes assumptions about these numbers into Flow/Apex logic (e.g. a future no-show-marking ticket) should re-read the record rather than assume the seeded value is final. | DHO/BA — ratify OQ-005/006/007 before BA_ARCH_CONFIRM or launch | Open |

| A-010 | 2026-07-12 | dev-mid (VS-03) | `VS_Session_Screen_DefineCapacity` (VS-03) does not scope the Service picker to only services actually offered by the selected Facility (i.e. it does not cross-check against an active `VS_Facility_Service__c` junction row) — the Facility and Service choice sets are two independent active-record lists. An MO could therefore create a `VS_Session__c` pairing a facility+service combination with no corresponding active `VS_Facility_Service__c` offering. No object-level validation rule was added to prevent this (ticket scope was in-flow input validation for date/time/capacity fields only, per its explicit instruction to use flow validation, not a new VR unless preferred). | If an MO picks a mismatched facility+service pair, a `VS_Session__c` (and later, VS-06 slots) could exist for an offering the facility doesn't actually provide, with no system-level block — a data-quality/UX gap, not a §3.4 integrity gap. | dev-mid/architect — decide at BA_ARCH_CONFIRM whether this needs a facility-scoped Service filter (two-screen flow re-work) or a `VS_Session__c` validation rule cross-checking `VS_Facility_Service__c` before F-001 ships | Open |
| A-011 | 2026-07-12 | dev-mid (VS-03) | The flow's `<start>` omits an explicit `runInMode` element, so it relies on the platform default (User Context — respecting the running MO's FLS/object permissions and sharing) rather than System Context. Cross-checked against `VS_MO_Facility_Admin.permissionset-meta.xml` (VS-04, already on disk): it grants Create+Edit on `VS_Session__c` and Read on `VS_Facility__c`/`VS_Service__c`, and every field this flow writes (`VS_Facility__c`, `VS_Service__c`, `VS_Session_Date__c`, `VS_Start_Time__c`, `VS_End_Time__c`, `VS_Total_Capacity__c`, `VS_Is_Drive_Day__c`, `VS_Status__c`) is marked editable there — so the assumption checks out on disk, but has NOT been proven in a live org (no dry-run run). | If a live-org FLS/CRUD check behaves differently than the static metadata suggests (e.g. profile-level restrictions stacking with the permission set), the flow's DML create would fail with an FLS/CRUD error surfaced through the fault screen. | dev-mid/human — confirm in the DE org at first dry-run/deploy with a user assigned only `VS_MO_Facility_Admin` | Open |

| A-012 | 2026-07-12 | dev-mid (VS-07) | `VS_Patient__c.VS_Gender__c` picklist values (Male/Female/Other/PreferNotToSay) are a dev-mid choice — design §2.3 says only "picklist, optional" and does not enumerate values, and no D-### or OQ ratifies a specific value set for gender. | If DHO/BA specify a different value set (e.g. a different label set or ordering), this is a picklist-value edit only, no redesign — but AC "picklist values match the design exactly" cannot be fully verified against this specific list since the design doesn't specify one | BA/DHO — confirm exact gender picklist values before BA_ARCH_CONFIRM or launch | Open |
| A-013 | 2026-07-12 | dev-mid (VS-07) | Design §2.3 lists `VS_Patient__c.VS_No_Show_Count__c` (Number) as part of the Patient object's field set (used later by VS-12 `VS_NoShowBatch`, which increments `VS_Patient__c.VS_No_Show_Count__c`). This ticket's explicit build instruction restricted VS-07 to "C1 fields + the match key (+ consent if designed)" and did not authorize adding a booking-behavior/audit field beyond that C1-minimal set, so **`VS_No_Show_Count__c` was deliberately NOT built in VS-07** even though the design places it on this object. VS-07's own AC list ("its only person fields are... exactly C1.1, nothing more") is satisfied either way since No_Show_Count isn't a C1 person attribute, but the field is still missing from disk relative to design §2.3. | VS-12 (`VS_NoShowBatch`, Sprint 2, dev-senior) depends only on VS-08 per the ticket register, not VS-07/this field — when VS-12 is implemented it will find `VS_Patient__c.VS_No_Show_Count__c` does not exist yet and will need to add it (or a new micro-ticket/field-add must precede VS-12) | pm-planner/architect — resolve at BA_ARCH_CONFIRM drift-check: either amend VS-07 (add the field via a follow-up) or confirm VS-12 owns adding it | **Resolved** (2026-07-12, dev-mid Bucket A — field added, see amendment below) |
| A-014 | 2026-07-12 | dev-mid (VS-08) | Field-history tracking was NOT enabled on `VS_Appointment__c` (object level or any field) in VS-08, even though VS-07 set a precedent of enabling object-level history tracking on `VS_Patient__c` as a REQ-054/A-006 audit-trail baseline beyond that ticket's literal AC. VS-08's explicit build instructions did not call for field history, and REQ-054 (read-audit) is explicitly not ticketed for F-001 per the sprint-plan gate note — so this may be intentional non-scope rather than a gap, but the inconsistency with VS-07's choice is worth a deliberate ruling rather than silent drift. | If BA_ARCH_CONFIRM expects the same audit-trail baseline on every Private-OWD person-adjacent object, `VS_Appointment__c` would need a follow-up field-history enable; if not, no impact | architect/BA — rule at BA_ARCH_CONFIRM drift-check on whether Private-OWD objects should consistently carry object-level history tracking | Open |

| A-015 | 2026-07-12 | dev-senior (VS-06) | `VS_SlotGenBatch.start()` selects only sessions dated **between today and the booking horizon inclusive** — i.e. it also EXCLUDES past-dated sessions (`VS_Session_Date__c >= today`), not just those beyond the horizon. VS-06's stated AC only requires the *beyond-horizon* exclusion (no slots past `BookingHorizonDays`); excluding past dates is a dev-senior design default (generating bookable slots for a session already in the past is pointless and could seed un-bookable/confusing rows). No D-### or AC mandates the past-date exclusion. | If the department expects the batch to backfill slots for past-dated sessions (e.g. for historical reporting or a late-created same-day session earlier than "now"), those sessions would silently generate no slots. Same-day sessions ARE included (date == today), so only strictly-past calendar dates are affected. | architect/BA — confirm at BA_ARCH_CONFIRM that past-dated sessions need no slot generation; if they do, relax the `>= today` bound in `start()` | Open |

**A-013 amendment (2026-07-12, dev-mid, Bucket A batched review fix) — RESOLVED:** `VS_Patient__c.VS_No_Show_Count__c` (Number(4,0), default 0, required=false) has been added at
`force-app/main/default/objects/VS_Patient__c/fields/VS_No_Show_Count__c.field-meta.xml`, closing the
gap this assumption flagged. It is a derived count of booking-behavior events, NOT a C1.1 person
attribute, so VS-07's "C1.1-exact" AC is unaffected either way (as originally noted). VS-12
(`VS_NoShowBatch`, Sprint 2, dev-senior) can now find and increment this field without needing a
preceding micro-ticket. Status changed **Open → Resolved**. (Row above left intact per append-only
discipline.)

**A-008 amendment (2026-07-11, dev-mid, VS-01 review fix-forward):** The original A-008 pattern (a Default Value formula on `VS_Facility_Service__c.VS_External_Id__c` referencing sibling lookup fields) was a **confirmed deploy-blocking defect**, not merely unverified: (1) a field cannot be both a formula and `externalId`/`unique` — the Metadata API rejects that combination outright; (2) default values cannot reference other fields on the same record at all (only `$User`/`$Profile`/global functions), so the intended mechanism was never possible. **RESOLVED BY REDESIGN**: `VS_External_Id__c` is now a plain Text(60) field (no `<formula>`), and a new before-save record-triggered flow, `VS_FacilityService_BeforeSave_SetExternalId` (`force-app/main/default/flows/`), sets it to `VS_Facility__c & "-" & VS_Service__c` on CREATE — before-save flows run before the Unique constraint is evaluated, so AC-3 (duplicate facility+service rejected) is now mechanically sound, not just declaratively plausible. **What still remains to verify (owner dev-mid/dev-senior, at `sf project deploy start --dry-run` + a manual duplicate-insert test in the DE org):** that the before-save flow populates `VS_External_Id__c` correctly on both UI saves and API/bulk inserts, and that the resulting Unique constraint violation surfaces a catchable, user-actionable error rather than a raw DML exception in later tickets that insert `VS_Facility_Service__c` programmatically.

| A-018 | 2026-07-12 | dev-senior (VS-06/VS-09, BLOCKER C) | The USER_MODE booking + slot-gen engines write to objects that **no role permission set currently grants**: `VS_BookingService.book()` does `insert as user` on `VS_Appointment__c` (no permset references Appointment — deferred VS-08/17/20), and `VS_SlotGenerationService` does `insert as user` on `VS_Slot__c` (all 5 permsets are Slot **read-only** — slots are system-generated). To make the USER_MODE tests executable WITHOUT weakening the rules/20 FLS enforcement, a dedicated TEST/CI harness permset **`VS_Booking_Engine_Test_Context`** was added and assigned only to the test users. Assumed: (a) this harness permset is acceptable to keep in the org purely for test/CI FLS context; (b) the real runtime grants — a booking permset granting `VS_Appointment__c` Create + field FLS, and a slot-gen automation/scheduler grant for `VS_Slot__c` Create — will be delivered by their owning tickets. | If left unaddressed, at RUNTIME no real Portal/staff user can call `book()` (USER_MODE insert on Appointment fails) and no scheduled/automation user can run `VS_SlotGenBatch` (USER_MODE insert on Slot fails) — a functional blocker, not just a test gap. If the harness permset is NOT wanted in the org, the tests need an alternative FLS-context strategy. | architect/pm — at BA_ARCH_CONFIRM: (1) assign `VS_Appointment__c` Create+FLS to the booking permset (VS-08/17/20); (2) define the slot-gen automation's `VS_Slot__c` Create grant (a scheduler/integration permset, since no human role should have it); (3) rule on keeping vs replacing `VS_Booking_Engine_Test_Context`. devops must add the harness permset to `manifest/deltas/SPRINT-1-phase2.xml` before the re-run. | Open |

| A-021 | 2026-07-13 | dev-mid (A-018 booking permset) | The test-harness permset `VS_Booking_Engine_Test_Context` grants `VS_Appointment__c` FLS on only 6 of the 8 fields `VS_BookingService.book()` sets (missing `VS_Slot__c`, `VS_Status__c`); investigated and found this is NOT an admin-profile-masked coverage gap — both fields are `<required>true</required>` on the object (`VS_Slot__c.field-meta.xml`, `VS_Status__c.field-meta.xml`), and the platform structurally disallows `<fieldPermissions>` entries on universally-required fields (object-level Create already covers them; `scripts/metadata-lint.js` rule #4 enforces this and would FAIL a permset that tried). The same pattern applies on `VS_Patient__c`: `VS_Full_Name__c`/`VS_Date_Of_Birth__c`/`VS_Mobile__c` are all `required=true` and were excluded from the new `VS_Booking_Capability` permset's field grants for the identical reason (only `VS_Gender__c`/`VS_Locality__c`/`VS_Pincode__c`/`VS_Email__c`/`VS_Match_Key__c` — all `required=false` — carry FLS). | If this reading is wrong (e.g. a future Salesforce release changes the required-field FLS rule, or `VS_Slot__c`/`VS_Status__c`/`VS_Full_Name__c`/`VS_Date_Of_Birth__c`/`VS_Mobile__c` are ever relaxed to `required=false`), the corresponding permset(s) need an added `<fieldPermissions>` entry — a one-line follow-up, not a redesign. | architect/dev — reconfirm at next permset touch if any of these 5 fields' `required` flag ever changes | Open |
| A-020 | 2026-07-12 | qa-engineer (`/qa-run B`) | Attempted to strengthen TC-010/TC-011 evidence beyond structural/API-level audit by creating fresh per-role test Users for a live login smoke test (per the run's own instruction to try this if feasible). Found: (1) only 1 of 4 "Salesforce" full-license user slots is available org-wide (`UserLicense` query — Total=4, Used=3), insufficient for the 5 production role personas needed; (2) `sf org create user` is documented as scoped to scratch orgs, and this is a persistent DE org (D-025) with unclear/unverified behavior for that command here. Declined to create a real, hard-to-reverse (deactivate-only, license-consuming) User on this shared persistent org without clearer authorization, given only partial coverage (1 of 5 roles) would be achievable anyway. Relied instead on live `ObjectPermissions`/`SetupEntityAccess` SOQL against the actual deployed grant graph (stronger than source-file reading, but still short of a real login/session smoke test). | If a future QA/dev session needs a true live per-role login proof (the natural next step after A-019 showed "FLS looks right on paper" was insufficient twice), it will hit the same 1-slot ceiling — either free up license slots (deactivate an unused system/integration user first) or use a browser-based "Login As" admin impersonation session (needs `enableAdminLoginAsAnyUser`, currently `false` per the live Security.settings retrieve, TC-023 evidence) instead of creating new Users. | devops/architect — decide the license/impersonation strategy before any live per-role smoke test is attempted | Open |
| A-019 | 2026-07-12 | qa-engineer (`/qa-run A`) | Empirically confirmed A-018 is WORSE than its original scope: the connected `AgentForceClaudeWorkFlow` admin session (the ONLY executable session obtainable via `sf` CLI/Tooling API in this environment — no browser/impersonation tool available) has **zero FLS beyond required fields** on every VS_ object (confirmed via `sobject describe` — e.g. `VS_Facility__c` shows only its 1 required field, `VS_Session__c` shows 8/12). Live Anonymous Apex therefore cannot invoke `VS_BookingService.book()` AT ALL, for ANY channel: the very FIRST SOQL inside `book()` (`SELECT...FOR UPDATE` on `VS_Session__c`, which reads `VS_Walk_In_Reserve_Count__c` regardless of channel) throws `No such column` under the admin session. Reproduced 3 ways: (1) a serial diagnostic call, (2) 20 genuinely-concurrent Tooling-API `executeAnonymous` HTTP calls (real overlapping curl timings captured) — all 20 failed identically, 0 reached capacity logic; (3) re-running the already-deployed `VS_BookingServiceWalkInTest` via `sf apex test run` reproduces the exact D-028 signature. Assigning the harness permset (`VS_Booking_Engine_Test_Context`) to the admin account was attempted and correctly REFUSED by the execution environment's permission system as an unauthorized RBAC change. The ONLY successful live-engine executions this run were the ALREADY-DEPLOYED, self-contained test classes (`VS_BookingServiceTest`, `VS_SlotGenBatchTest`), which internally create their own FLS-bearing user and invoke `System.runAs()` — legal only inside `@IsTest` methods, not live Anonymous Apex. | Blocks TC-001/002/003 (§3.4 concurrency — the crown jewel) and any fresh-org-data variant of TC-004/005/006/022/015-020 for every future QA session against this org, not just this run, until resolved. Walk-in/mixed-channel §3.4 no-overbooking remains **completely unproven**, which is release-relevant. | devops/architect — before the next Run A attempt, provide ONE of: (a) an authenticated session (access token) for the dedicated "QA Load Test Harness" user (e.g. a Connected App username-password flow, or an interactive admin "Login As"); (b) the test-plan §3 fallback — a temporary QA-owned Apex REST endpoint wrapping `book()`, deployed and later removed by devops; (c) confirm whether this is intended to also block real production users (A-018's original scope) and route the fix there, which would incidentally unblock QA too. | Open |
