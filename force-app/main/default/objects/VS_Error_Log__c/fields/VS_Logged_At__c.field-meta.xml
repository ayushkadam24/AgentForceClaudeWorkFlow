<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VS_Logged_At__c</fullName>
    <label>Logged At</label>
    <description>Timestamp the error was written, set by the logging flow/action (typically $Flow.CurrentDateTime or Now()) rather than relying solely on CreatedDate, so the value is explicit and portable if records are ever migrated.</description>
    <type>DateTime</type>
    <required>false</required>
</CustomField>
