<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>VS Appointment</label>
    <pluralLabel>VS Appointments</pluralLabel>
    <description>F-001 slot-booking-core (VS-08, EP-03 -- §3.4 slot-integrity booking, crown-jewel epic). The booking record that VS_BookingService.book() (VS-09, Apex, dev-senior) inserts for every channel (Portal/Chat/Staff/WalkIn). This ticket builds SCHEMA ONLY -- no booking logic, no VS_Booking_Reference__c generation, no VS_Slot__c/VS_Session__c counter increment (all VS-09 Apex per D-019/D-020). Relationships to Patient/Slot/Session/Facility/Service are all Lookup (never Master-Detail): design §2.4 requires VS_Slot__c -> VS_Appointment__c to be reparentable so VS-11's reschedule() can move an appointment to a different slot/session without a delete+recreate, and requires VS_Patient__c -> VS_Appointment__c to be a Lookup because patient retention (>=10 yr if vaccinated) and appointment retention (3 yr, Annexure C4) are different lifecycles that a Master-Detail cascade-delete would incorrectly couple. No clinical/diagnosis fields exist anywhere on this object (REQ-045 -- health data here is limited to the booking/appointment lifecycle only). NO Aadhaar field, no Aadhaar-shaped field, anywhere. OWD is Private (Annexure C5 -- booking is person-adjacent data). Retention class (Annexure C4): bookings 3 yr, purge/archive governed by VS_RetentionPurgeBatch (VS-21) honoring the patient's longest-linked record (design §6.5) -- this object's retention class does not itself force early patient purge.</description>
    <deploymentStatus>Deployed</deploymentStatus>
    <sharingModel>Private</sharingModel>
    <enableActivities>false</enableActivities>
    <enableBulkApi>true</enableBulkApi>
    <enableFeeds>false</enableFeeds>
    <enableHistory>false</enableHistory>
    <enableLicensing>false</enableLicensing>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <enableSharing>true</enableSharing>
    <enableStreamingApi>true</enableStreamingApi>
    <nameField>
        <displayFormat>APT-{00000}</displayFormat>
        <label>Appointment Record</label>
        <trackHistory>false</trackHistory>
        <type>AutoNumber</type>
    </nameField>
</CustomObject>
